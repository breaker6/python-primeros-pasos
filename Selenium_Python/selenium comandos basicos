Instrucciones básicas

Importar libreria:
from selenium import webdriver
Cargar el navegador en que haremos las pruebas:
driver = webdriver.Chrome()
Cargamos la URL que queramos:
driver.get("URL")
Sacamos el título de la página
driver.title

Importamos la librería By que nos será util para buscar elementos:
from selenium.webdriver.common.by import By
Buscamos el elemento con la etiqueta title:
driver.find_element(By.TAGNAME,"title")
Igual que el anterior pero sacando el texto:
driver.find_element(By.TAGNAME,"title").text
Buescamos el elemento con el ID title:
driver.find_element(By.ID,"title")
driver.find_element(By.ID,"title").text
Buscamos y damos click a un Link buscando el texto con el que está escrito:
driver.find_element(By.LINK_TEXT,"Texto enlace").click()

Ejemplo de rellenado de un formulario de usuario y contraseña (con comentarios):
from selenium import webdriver #Importamos el webdriver
from selenium.webdriver.common.by import By #Importamos la libreria By para buscar elementos
from selenium.webdriver.common.keys import Keys #Importamos la libreria Keys que nos permitirá rellenar campos
driver = webdriver.Chrome() #Abrimos el navegador
driver.get("URL") #Abrimos la URL
usuario = driver.find_element(By.NAME,"username") #Buscamos el elemento con el nombre username y copiamos su ubicación en la variable usuario
usuario.send_keys("fbeltran") #Introducimos el texto con el usuario
password = driver.find_element(By.NAME,"password") #Hacemos lo mismo con la contraseña
password.send_keys("contraseña")
driver.find_element(By.NAME,"bEntrar").click() #Pulsamos en el botón para iniciar sesión

Buscar un elemento por su clase:
# Supongamos que la clase del elemento que deseas buscar es 'clase_ejemplo'
element = driver.find_element_by_class_name('clase_ejemplo')  # Si esperas un solo elemento
# O si esperas múltiples elementos con la misma clase:
elements = driver.find_elements_by_class_name('clase_ejemplo')

Buscar un elemento con varias clases:
# Busca el elemento con varias clases
element = driver.find_element_by_css_selector('.clase1.clase2')
# Busca todos los elementos que tienen las clases 'clase1' y 'clase2'
elements = driver.find_elements_by_css_selector('.clase1.clase2')
# Itera a través de los elementos y realiza acciones con cada uno de ellos
for element in elements:
    texto = element.text
    print(texto)

