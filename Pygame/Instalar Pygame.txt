Para instalar pygame tenemos que ejecutar este comando en el terminal de windows:

python -m pip install -U pygame==2.1.3 --user

Si no lo reconoce, escribimos simplemente python en el terminal e instalamos python desde la tienda de Windows. Despues volvemos a ejecutar el comando anterior y ya lo instalará